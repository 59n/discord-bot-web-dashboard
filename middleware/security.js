function setupSecurity(app) {
    // Add any additional security middleware here
    // For now, this is handled in the main middleware setup
    console.log('✅ Security middleware configured');
}

module.exports = { setupSecurity };
